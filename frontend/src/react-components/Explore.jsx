import React, { useState } from 'react'

const Explore = ({ optimizedImages, initialData }) => {
  const [data, setData] = useState(initialData)
  const [isLoading, setIsLoading] = useState(false)

  // if (isLoading) {
  //   return <div>Loading...</div>
  // }

  return <div>yoyo</div>
}

export default Explore
