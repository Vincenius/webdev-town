
const handler = async (req, res) => {
  // fetch resources by time
  // upload images to s3
  // generate html (use body for description)

  res.status(200).json({ result: 'todo' })
}

export default handler
