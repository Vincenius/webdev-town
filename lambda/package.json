{
  "name": "webdev-town-api",
  "version": "1.0.0",
  "scripts": {
    "dev": "lambda-local -l index.js -h handler --watch 8008"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "mongodb": "^5.7.0"
  }
}
